package com.hbm.space.dim.trait;

/**
 * Marker trait indicating the body has been compromised/infected.
 * No additional data needed — presence of this trait is sufficient.
 */
public class CBT_Compromised extends CelestialBodyTrait {
    // Marker trait — no additional data needed
}
